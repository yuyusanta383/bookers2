<h2 class="mt-3">New book</h2>
<%= form_with model:book, local:true do |book| %>
<h4>Title</h4>
<%= book.text_field :title %>

<h4>Opinion</h4>
<%= book.text_area :body %>

<br><%= book.submit 'Create Book' %></br>
<% end %>
