<h1>Editing Book</h1>
<% if @book_find.errors.any? %>
  <%= @book_find.errors.count %>error prohibited this obj from being saved:
  <ul><% @book_find.errors.full_messages.each do |message| %>
    <li><%= message %></li>
   <% end %>
  </ul>
<% end %>
<%= form_with model:@book_find,local:true do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>
<h4>Opinion</h4>
<%= f.text_area :body %>

<br><%= f.submit 'Update Book' %></br>

<% end %>
<th>
<%= link_to "Show", book_path(@book_find) %>|
<%= link_to "Back", books_path %>
</th>